import React from "react";
import style from "./style.module.css"

export default function HomePage() {
    return (
    <div className={style.home}>
        <button className={style.getStart}>Get Start</button>
    </div>
)
}